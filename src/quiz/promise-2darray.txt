Explain the code execution based on the order of the messages logged in src/quiz/promise-2darray.txt.

The code with be asynchronous sum calculation for valid inputs but it will reject 
if the inputs are not valid. The logging messages indicating different stages of the
execution process. 